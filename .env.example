# Chess Engine Service Configuration

# Engine Settings
# Optional path to the engine binary. If not set, will use built-in path.
ENGINE_PATH=

# Name of the chess engine (default: stockfish)
ENGINE_NAME=stockfish

# Version of the chess engine (default: 17.1)
ENGINE_VERSION=17.1

# Operating system for the engine binary (default: linux)
ENGINE_OS=linux

# Name of the engine binary file (default: stockfish)
ENGINE_BINARY=stockfish

# MCP Server Settings
# Host address for the MCP server (default: 127.0.0.1)
MCP_HOST=127.0.0.1

# Port for the MCP server (default: 9000)
MCP_PORT=9000

# Logging Settings
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO
