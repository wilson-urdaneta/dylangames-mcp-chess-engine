# Engine Binary Configuration
# Primary path to the engine binary
ENGINE_PATH=/path/to/your/engine/binary

# Fallback configuration (used if ENGINE_PATH is not set or invalid)
# Default: stockfish
ENGINE_NAME=stockfish

# Default: 17.1
ENGINE_VERSION=17.1

# Default: linux
ENGINE_OS=linux

# Default: stockfish (include .exe for Windows)
ENGINE_BINARY=stockfish

# MCP Server Configuration
# Host address for the MCP server to bind to
# Default: 127.0.0.1
MCP_HOST=127.0.0.1

# Port number for the MCP server to listen on
# Default: 8001
MCP_PORT=8001
